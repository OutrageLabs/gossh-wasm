# Copy to .env (or export in shell) before running docker compose tests.
# Use explicit, pinned image tags or digests.

SSHSERVER_IMAGE=lscr.io/linuxserver/openssh-server:version-10.0_p2-r0
WSPROXY_GO_IMAGE=golang:1.25.6-alpine3.22
WEBSERVER_IMAGE=nginx:1.29.2-alpine

